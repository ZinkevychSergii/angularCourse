import { BigFileDirective } from './big-file.directive';

describe('BigFileDirective', () => {
  it('should create an instance', () => {
    const directive = new BigFileDirective();
    expect(directive).toBeTruthy();
  });
});
