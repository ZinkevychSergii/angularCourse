import { OmitSpaceDirective } from './omit-space.directive';

describe('OmitSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new OmitSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
